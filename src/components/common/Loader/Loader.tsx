import { CircularProgress } from "@mui/material";

import * as Styled from "./Loader.styled";

const Loader = () => (
  <Styled.MyContainer>
    <CircularProgress />
  </Styled.MyContainer>
);

export default Loader;
